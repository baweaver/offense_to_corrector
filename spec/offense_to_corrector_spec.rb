# frozen_string_literal: true

RSpec.describe OffenseToCorrector do
  it "has a version number" do
    expect(OffenseToCorrector::VERSION).not_to be nil
  end

  # Specs TBD, very much experimental at the moment
  it "does something useful" do
    expect(true).to eq(true)
  end
end
